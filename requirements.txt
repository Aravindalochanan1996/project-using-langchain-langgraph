# Core LLM orchestration
langgraph>=0.2.0
langchain>=0.2.0
langchain-openai>=0.1.0

# Transitive dependency of langchain_core.runnables — pinned explicitly
# because some environments (e.g. fresh venvs, restricted pip mirrors)
# don't always resolve it automatically, causing:
#   ImportError: No module named 'uuid_utils'
uuid_utils>=0.9.0

# LLM providers
openai>=1.30.0

# Schema validation
pydantic>=2.0.0

# Prompt file loading
pyyaml>=6.0

# Azure AI services (optional — only needed for production Azure integration)
# azure-ai-documentintelligence>=1.0.0
# azure-core>=1.30.0
# azure-identity>=1.16.0

# Testing
pytest>=8.0.0

# Environment management
python-dotenv>=1.0.0
